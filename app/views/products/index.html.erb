<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Store</th>
      <th>Category</th>
      <th>Price</th>
      <th>Rate</th>
      <th>Quantity</th>
      <th colspan="3"></th>
      <th>Buy Now </th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.store.name %></td>
        <td><%= product.category.name %></td>
        <td><%= product.price %></td>
        <td><%= product.rate %></td>
        <td><%= product.quantity %></td>
        <td><%= link_to 'Show', product %></td>
        <%# <td><%= link_to 'add To cart ',  %></td>

        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td>

        <%= form_with url: "/cart/items", method: :post do |form| %>
            <%# <% hidden_field_tag :productId ,product.id%> 
       <%# <%=  form_with(model: @order_products, url: order_products_path) do |form| %> %>
     <%# <% number_field_tag :quantity ,1 %> 
         <%# <% submit_tag "Add to cart" %> 
            <%= form.hidden_field :product_id, value: product.id %>
            <%= form.number_field :quantity, value: "1" %>
       
        <%= form.submit "Add to cart" %>

 <%end%>
        
        
        
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
