<p id="notice"><%= notice %></p>
 <% if @store %>
 <%= render 'stores/sideMenu' %>
 <%end %>
<div class="container store-content">
 <div class="container d-flex justify-content-center ">
 <div class="row" style="margin-bottom:10px">
 <button class="btn btn-success">ADD New Product</button>
 </div>
    <div class="row">
   
    <% @products.each do |product| %>

        <div class="col-md-3">
            <div class="product-wrapper mb-45 text-center">
                <div class="product-img"> <a href="#" data-abc="true"> <%= image_tag url_for(product.images[0]) %> </a> <span class="text-center"><i class="fa fa-rupee"></i><%= product.name%></span>
                    <div class="product-action">
                        <div class="product-action-style"> <a href="#"> <i class="fa fa-eye"></i> </a>  <a href="#"> <i class="fa fa-shopping-cart"></i> </a> </div>
                    </div>
                     <% if @store %>
                     <% if can? :read, @store %>
                      <div class="product-action">
                        <div class="product-action-style"> <a href="<%= store_product_path(@store , product)%>"> <i class="fa fa-eye"></i> </a> <a href="<%= edit_store_product_path(@store , product)%>"> <i class="fa fa-pen"></i> </a>  <a href="#"> <i class="fa fa-trash"></i> </a> </div>
                    </div>
                    <%end%>
                    <%end%>
                </div>
            </div>
        </div>
  
    
     
    <%end%>
       </div>
  </div>

</div>
<br>


